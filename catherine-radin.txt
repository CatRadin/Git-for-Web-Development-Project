    1. What is Semantic HTML? 

semantic HTML is an element that describes it meaning to both the browers and developer. An example would be <heading> as it more desciptive then say <div>.

    2. What is HTML used for? 

Html is used for create sections, paragraphs and so on for webpages and applications. Html makes it eaiser for you to style the blocks of data.

    3. What is an attribute and where do we put it? 

An attribute is put after the tag of an HTML for instance in <img> you would put src (source) like this <img src="insert-link-here">

    4. What is the h1 tag used for? How many times should I use it on a page?

An h1 tag is the most important information on the page. You can use it more than once however, its bad pratice. You should only use it once.

    5. Name two tags that have required attributes

<img src=""> (image path) & <a href="">Blah Blah</a> (a link to a url)

    6. What do we put in the head of our HTML document? 

This is where I could put the title for the page. This is the text that will be displayed in the tab.

    7. What is an id? 

An id is a unique selector. You would call the Id in the css with a #name-here. Ids are unique and can only be used once, the name that is. You can have multiple ids in your code but the names are unique. Like <div id="first"> is unique and you cannot name another id "first".

    8. What elements can I add an id to? 

You can add them to semantic and non semantic HTML like div, selection, h1-h6 and so on.

    9. How many times can I use the same id on a page? 

Once.

    10. What is a class? 

A class is a selector. You can use the same name class in the same code. For instance <div class="test"> could be used again later in the code. Unlike an ID.


    11. What elements can I add a class to? 

Semantic and non semantic HTML, div, h1-h6, selection and so on.

    12. How many times can I use the same class on a page? 

As many times as you want.

    13. How do I get my link to open in a new tab?

To get a link to open in a new tab you would make an anchor tag <a href="" target="_blank">Blah</a>

    14. What is the alt attribute in the image tag used for? 

This is used to discribe an image. This helps the blink as the image is descibed to them. It is also displayed if the image can't be viewed for some reason.

    15. How do I reference an id?

#

    16. What is the difference between a section and a div

A section is semantic HTML. It is more descriptive then div.

    17. What is CSS used for? 

CSS is used to style the webpage.

    18. How to we select an element? Example - every h2 on the page

To select an element you would call it in the css. To style the h2 elements you would 
h2 {
color:red;
}

    19. What is the difference between a class and an id? - Give me an example of when I might use each one

Class can be reused in the code where an ID can't. Use an ID if you are certain you won't want to name somthing the same.

    20. How do we select classes in CSS?

You use .name-here

    21. How do we select a p element with a single class of “human”?

p.human {

}
    22. What is a parent child selector? When would this be useful? 

> you can use this when you want to select and style ever element where somthig is the partent of it.

    23. How do you select all links within a div with the class of sidebar?

div.sidebar {
}

    24. What is a pseudo selector?

Used to define a special state of an element. Like if you want somthing to change when they hover over somthing. you could do 

p:hover {
}

    25. What do we use the change the spacing between lines?

line-height: 

    26. What do we use to change the spacing between letters?

letter-spacing:

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?

text-transform: 

    28. How do I add a 1px border around my div that is dotted and black?

div {

border: dotted black

}
    29. How do I select everything on the page? 

* {
}
    30. How do I write a comment in CSS?

/* comment */

    31. How do I find out what file I am in, when I am using the command line? 

You can read where you are in the text before you type.

    32. Using the command line - how do I see a list of files/folders in my current folder?

ls

    33. How do I remove a file via the command line? Why do I have to be careful with this? 

rm. You need to be careful because it doesn't promt to make sure you want to do it.

    34. Why should I use version control? 

Version control is important because you can rollback to past versions incase you made a mistake.

    35. How often should I commit to github?

often so you don't lose your work.

    36. What is the command we would use to push our repo up to github? 

git push u- origin catherine-radin

    37. Walk me through Lambda's git flow. 

First you fork then you clone. Then you make a branch. You can then work on the project. Then you add then commit and finally push. You then make a pull request to yourself to turn in the assignment.

Stretch Questions

    1. What is the difference between an inline element and a block element?

Inline elements do not force a new line in the document. Block elements typically cause a line break to occur.

    2. What happens when an element is positioned absolutely? 

This is an element whos computed postion will not move. It is fixed.

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 

I think this is where you would use flexbox or a grid.

    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default'

Div, h1-h6, p

inline-block, display

button



    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 

All HTML elements are considerd boxs. It consists of: margins, borders, padding, and the actual content. 

The "FIX" is
box-sizing: border-box;
This helps because it actually becomes the size you make it.